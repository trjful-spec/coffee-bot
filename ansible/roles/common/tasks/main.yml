---
- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install packages
  apt:
    name:
      - git
      - rsync
      - sqlite3
      - python3
      - python3-pip
      - python3-venv
    state: present
